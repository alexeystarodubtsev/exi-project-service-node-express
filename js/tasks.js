const Cookies       = require('cookies');
const express        = require('express');
const db            = require('./config');
let ObjectID        = require('mongodb').ObjectId;

module.exports= {
   
    add: function(req, res)
    {
        //еще не успел сюда ничего написать
    }

}